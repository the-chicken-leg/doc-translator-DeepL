[project]
name = "doc-translator-deepl"
version = "0.1.0"
description = "Translate documents using the DeepL API."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "deepl>=1.28.0",
]
