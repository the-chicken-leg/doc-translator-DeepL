[project]
name = "doc-translator-deepl"
version = "0.1.0"
description = "Translate a document using the DeepL API."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "deepl>=1.29.0",
]
